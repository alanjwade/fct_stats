# Copy to .env and fill in real values, then: docker compose up -d
TZ=America/Denver

# Optional: override the app's built-in analytics defaults.
# ANALYTICS_SECRET=change-me
# VISITOR_SALT=change-me
